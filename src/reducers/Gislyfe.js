//define the initial state
const initialState = {
    layers: [],
}

//define a reducer with an initialized state action
function Gislyfe(state = initialState, action) {
    //eventually adding logic to handle create, update, and delete
    return state
  }

  export default Gislyfe